server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001:7001/eureka,http://eureka7002.com:7002/eureka
#ribbon:
#  ReadTimeout: 5000
#  ConnectTimeout: 5000
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
logging:
  level:
    cn.ga.service.PaymentFeignService: debug
