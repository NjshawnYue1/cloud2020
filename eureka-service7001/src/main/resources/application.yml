server:
  port: 7001

eureka:
  instance:
    hostname: eureka7001.com #实例的名字
  client:
    register-with-eureka: false #不注册自己
    fetch-registry: false #不需要检索服务
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/